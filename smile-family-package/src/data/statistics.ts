import type { Statistic } from '@/types';

/**
 * ATENÇÃO: valores TEMPORÁRIOS / DEMONSTRATIVOS.
 * Substituir pelos números reais da clínica antes de publicar.
 * Não representam dados verificados.
 */
export const statistics: Statistic[] = [
  { value: 5000, suffix: '+', label: 'Pacientes atendidos' }, // TEMP — substituir
  { value: 12000, suffix: '+', label: 'Tratamentos realizados' }, // TEMP — substituir
  { value: 10, suffix: '+', label: 'Anos de experiência' }, // TEMP — substituir
  { value: 8, suffix: '', label: 'Profissionais' }, // TEMP — substituir
];
